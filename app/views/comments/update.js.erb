$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

<% if @comment_updated %>
  $('.comment[data-id=<%=@comment.id%>]').replaceWith("<%=j render partial: 'comment', locals: { comment: @comment, post: @comment.post, topic: @topic } %>");
<% end %>

// data-id, or you can append it to the id #comment-55
